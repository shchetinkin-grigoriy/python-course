Понравилось:
- проверка четности по [::2]
- строки всегда обрезать
- перевод аргументов во влот через map(float, argv[2:])
- enumerate раскладывать на i, item
- элемент с индексом -1 = последний
- генераторы можно создавать на базе листа - без лишних скобок
 * новые строки в рамках длинного условия через \
#файлы
- enumetare(file_1.readline(), start=1) - для нумерации строки
- enumerate([1,2]) - возвращает особый enumerate object
- os.path.join(DIR , '2.txt') - способ создания пути
- сложение списков через zip


